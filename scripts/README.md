# Scripts

Các script nhỏ dùng cho local dev, đồng bộ dữ liệu và việc lặp lại trong dự án.

- Local run scripts (e.g., `run_backend.ps1`, `run_frontend.ps1`)
- Sync utilities (e.g., `sync_s3.ps1`)
- Maintenance scripts
- Small helper commands

