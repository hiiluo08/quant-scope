# Data

Thư mục này để chứa dữ liệu local trong lúc làm việc. Không commit file data lên Git.

## Structure

- `raw/` cho dữ liệu gốc tải về
- `processed/` cho dữ liệu đã chuẩn hóa

## Lưu ý

- Parquet, CSV, SQLite local có thể để ở đây
- Secrets và API keys không được để trong thư mục này
